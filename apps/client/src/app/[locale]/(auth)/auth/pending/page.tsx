import { InnerAccountPendingPage } from "./component";

export const metadata = {
  title: "Pending Account - SnailyCAD",
};

export default function PendingAccountPage() {
  return <InnerAccountPendingPage />;
}
