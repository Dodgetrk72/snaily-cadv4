import { InnerAccountTempPassword } from "./component";

export const metadata = {
  title: "Change Account Password - SnailyCAD",
};

export default function AccountTempPassword() {
  return <InnerAccountTempPassword />;
}
